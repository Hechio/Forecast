default_platform :ios

platform :ios do
  before_all do
    setup_circle_ci
  end

  desc "Runs all the tests"
  lane :test do
    ENV['SCAN_COLLECT_LOGS'] = 'false'
  scan(
    scheme: "ForecastTests",
    devices: ["iPhone 13 Pro"]
  )
  end
end
